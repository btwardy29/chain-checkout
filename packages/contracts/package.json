{
  "name": "@chain-checkout/contracts",
  "version": "0.1.0",
  "scripts": {
    "build": "hardhat compile",
    "deploy:base-sepolia": "hardhat run scripts/deploy.ts --network baseSepolia",
    "deploy:local": "hardhat run scripts/deploy.ts --network localhost",
    "mint:test-token": "hardhat run scripts/mint-test-token.ts --network baseSepolia",
    "test": "hardhat test",
    "lint": "hardhat compile"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.3.0",
    "dotenv": "^16.5.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.0.0",
    "hardhat": "^2.24.0",
    "typescript": "^5.8.3"
  }
}
